import { LucideIcon } from "lucide-react";

interface SoftSkillType {
  title: string;
  icon: LucideIcon;
  description: string;
  delay: number;
  moreDetails: string;
}

export type { SoftSkillType };
